{
  "name": "pocket",
  "version": "0.1.0",
  "description": "A simple state manager.",
  "main": "index.js",
  "author": "Dustin Dowell <dustindowell2@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prep": "rm -rf public && mkdir public && cp test/index.html public/index.html",
    "rollup": "rollup test/actions.js -o public/test.js -f iife -m",
    "test": "npm run prep && npm run rollup && open public/index.html"
  },
  "devDependencies": {
    "rollup": "^1.10.0"
  },
  "standard": {
    "globals": [
      "requestAnimationFrame"
    ]
  }
}
